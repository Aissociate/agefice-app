services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - sqlite_data:/app/prisma
    environment:
      - DATABASE_URL=file:/app/prisma/prod.db
      - NODE_ENV=production
    env_file:
      - .env.production

volumes:
  sqlite_data:
